# Terminal 1 - Backend
cd C:\Dev\zampos\backend
venv\Scripts\activate
python -m uvicorn main:app --reload

# Terminal 2 - Frontend  
cd C:\Dev\zampos\frontend
npm run dev

🚀
//ngrok
ngrok http 3000
//ngrok http 8000

# Make your changes locally, test, then:
git add .
git commit -m "feat: your feature name"
git push
# Vercel + Render both auto-deploy 

swagger
http://127.0.0.1:8000/docs

git checkout v2-sms-testing
git checkout main 

npm run build

# You're on develop branch
git branch
# * develop
#   main

# Switch to main (production)
git checkout main

# Switch back to develop (testing)
git checkout develop


# Stay on develop for all new features
git checkout develop

# Make changes to frontend code
# Test locally first!
cd frontend
npm run build  # Must succeed

# Commit and push to develop
git add .
git commit -m "feat: rate service with Binance fallback, realistic 1.35M ZMW/BTC static rate"
git push origin develop
git checkout main
git pull origin main
git merge develop -m "Merge develop: rate improvements and UI fixes"
git push origin main
git checkout develop  # Switch back to develop for continued work
develop branch (test)  →  Vercel preview URL (test only)
         ↓
   (merge when ready)
         ↓
main branch (production) → zampos.vercel.app (live users)


# 1. Generate top-up invoice
curl -X POST http://localhost:8000/owner/breez-topup?amount_sats=10000

# 2. Pay the returned invoice from your wallet
# 3. Verify balance
curl http://localhost:8000/owner/breez-status
# → {"balance_sats": 10000, "status": "online"}